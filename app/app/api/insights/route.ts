// Insights route handler — returns precomputed spending breakdowns for the authenticated user
export async function GET() {
  return Response.json({ message: "Not implemented" }, { status: 501 });
}
